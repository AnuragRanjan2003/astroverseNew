import 'package:flutter/material.dart';

class UserLoginScreenLandscape extends StatelessWidget {
  final BoxConstraints cons;
  const UserLoginScreenLandscape({super.key, required this.cons});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
