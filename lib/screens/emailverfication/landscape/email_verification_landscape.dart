import 'package:flutter/material.dart';

class EmailVerificationLandscape extends StatelessWidget {
  final BoxConstraints cons;
  const EmailVerificationLandscape({super.key, required this.cons});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
