import 'package:flutter/material.dart';

class PersonItems extends StatelessWidget {
  const PersonItems({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('items'),);
  }
}
